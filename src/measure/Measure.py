#conding:utf-8
#interface for measures

class Measure:
	
	def __init__(self):
		self.measureFile = "./data/measureFile.csv"
	
	# make a measure and write it in the measure file
	def makeMeasure(self,source_prob, dest_prob):
		return
	
	# make all the measure for a given number of probes number
	def measureGraph(self,prob_nb):
		return
